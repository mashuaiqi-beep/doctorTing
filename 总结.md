# doctorTing 项目总结（面试准备版）

## 1. 项目一句话介绍

doctorTing 是一个基于 FastAPI 和大模型能力实现的医疗问诊分诊 Agent Demo。它支持用户用自然语言描述症状，系统进行多轮追问、红旗症状识别、风险等级判断、知识库检索、科室推荐，并最终返回结构化的分诊建议。

这个项目的定位不是替代医生诊断，也不是完整医院 HIS/EMR 系统，而是一个面向实习、求职和 Agent 工程能力展示的后端项目。

面试时可以这样说：

> 我做了一个医疗问诊分诊 Agent，核心是把用户自然语言症状描述转成结构化问诊状态。项目用 FastAPI 提供接口，用 LLM 做症状抽取和建议生成，用规则加 LLM 做红旗症状识别，用 ChromaDB 做本地医学知识库检索，并通过 session 维护多轮问诊上下文。医疗场景下我重点做了安全边界：不诊断、不开药，高风险症状优先建议线下或急诊。

## 2. 当前项目已经做到什么程度

当前项目已经不是一个简单的 FastAPI Demo，而是有一条完整可运行的问诊主链路：

```text
用户输入症状
  -> POST /triage/start
  -> LLM 抽取症状、缺失信息、下一轮追问
  -> 规则 + LLM 识别红旗症状
  -> 创建 session
  -> POST /triage/continue
  -> 基于 session 更新摘要、症状、风险和追问
  -> POST /triage/evaluate
  -> 检索本地医学知识库
  -> 根据风险和症状推荐科室
  -> LLM 结合问诊状态和知识库生成分诊建议
  -> 返回结构化 JSON
```

当前已经完成的能力：

- FastAPI 应用入口、健康检查和三条核心问诊接口。
- Pydantic 请求/响应模型，保证接口输入输出结构稳定。
- LLM 服务层，封装 OpenAI-compatible Chat Completions 调用。
- Prompt 管理层，把首轮问诊、继续问诊、红旗识别、最终评估的 Prompt 集中管理。
- JSON 解析兜底，兼容模型返回 Markdown 代码块或夹带解释文本的情况。
- 内存版 `SessionStore`，支持多轮问诊上下文维护。
- Redis 版 `RedisSessionStore` 雏形，后续可替换内存 session。
- 规则版红旗症状识别，保证 LLM 不可用时仍能识别明显高风险。
- ToolService 工具层，封装症状抽取、红旗检查、知识检索和科室推荐能力。
- 本地 Markdown 医疗知识库。
- ChromaDB 向量知识库服务，支持从 Markdown 构建索引并按语义检索。
- 模拟挂号工具，根据症状和风险等级推荐科室。
- pytest 自动化测试，覆盖接口主流程、红旗风险、未知 session、RAG 检索和工具行为。

## 3. 技术栈和模块分层

项目技术栈：

- 后端框架：`FastAPI`
- 数据校验：`Pydantic`
- LLM 接口：`openai` SDK，使用 OpenAI 兼容接口，默认配置 DeepSeek compatible endpoint
- 工具调用：OpenAI-compatible tools/function calling schema
- 知识库：Markdown 文档 + ChromaDB 持久化向量库
- Embedding：`shibing624/text2vec-base-chinese`
- 会话存储：内存版 `SessionStore`，预留 Redis 版 `RedisSessionStore`
- 测试：`pytest`、`fastapi.testclient`
- 后续编排预留：`LangGraph`

核心目录职责：

```text
app/main.py                  FastAPI 应用入口
app/api/triage_api.py        HTTP 路由层
app/schema/triage_schema.py  Pydantic 请求/响应模型
app/service/triage_service.py 核心业务编排
app/service/llm_service.py   LLM 调用封装
app/service/tool_service.py  工具层统一入口
app/service/session_store.py 内存会话存储
app/service/redis_session_store.py Redis 会话存储预留
app/service/vector_knowledge_service.py ChromaDB 向量知识库
app/core/prompt_manager.py   Prompt 模板管理
app/core/risk_rules.py       红旗症状规则
app/tools/                   可复用工具函数
app/agent/triage_graph.py    LangGraph 工作流预留
data/knowledge/              Markdown 医疗知识库
tests/                       自动化测试
```

面试表达重点：

> 我没有把逻辑全部写在 API 函数里，而是拆成 API 层、Schema 层、Service 层、Core 层、Tools 层和 Knowledge 层。API 层只负责 HTTP 入参和响应，`TriageService` 负责编排业务流程，`LLMService` 专门处理模型调用，`ToolService` 封装工具能力。这样后续替换 session 存储、升级 RAG 或接入 LangGraph 时，不需要大改接口层。

## 4. 三条核心接口

### 4.1 `POST /triage/start`

职责：开始问诊。

输入：

```json
{
  "user_input": "我发热两天了，还一直咳嗽"
}
```

输出：

- `session_id`：会话 ID
- `symptoms`：已识别症状
- `missing_fields`：缺失的关键问诊信息
- `next_question`：下一轮追问
- `risk_level`：风险等级
- `red_flags`：命中的红旗症状

内部流程：

```text
API 接收 user_input
  -> TriageService.start_triage
  -> LLMService.extract_triage_info
  -> RiskControlService.check_risk
  -> ToolService.check_red_flags
  -> _merge_risk 合并风险
  -> SessionStore.create_session
  -> 返回结构化响应
```

### 4.2 `POST /triage/continue`

职责：继续已有问诊。

输入：

```json
{
  "session_id": "xxx",
  "user_input": "最高 38.5 度，没有呼吸困难"
}
```

输出：

- `updated_summary`：更新后的病情摘要
- `next_question`：下一轮追问
- `need_more_info`：是否还需要继续追问

关键点：

- 根据 `session_id` 读取历史状态。
- 把用户新回答追加到 `messages`。
- LLM 根据旧 session 和新回答更新症状、摘要和缺失信息。
- 重新检查当前输入是否包含红旗症状。
- 如果历史已经是高风险，后续回答不能把风险降级。

面试表达：

> 多轮问诊不是靠模型自己“记住”上下文，而是后端通过 session 主动维护状态。每一轮用户回答都会写入消息历史，并更新症状、摘要、缺失字段、风险等级和红旗症状。

### 4.3 `POST /triage/evaluate`

职责：生成最终分诊建议。

输入：

```json
{
  "session_id": "xxx"
}
```

输出：

- `summary`：病情摘要
- `risk_level`：最终风险等级
- `red_flags`：红旗症状
- `department`：推荐科室
- `advice`：分诊建议
- `references`：知识库来源

内部流程：

```text
读取 session
  -> 从 symptoms 或 summary 构造 query
  -> ToolService.search_knowledge 检索知识库
  -> ToolService.book_appointment 推荐科室
  -> LLMService.evaluate_triage 生成建议
  -> 返回风险、科室、建议和 references
```

## 5. 多轮问诊和 Session 设计

当前使用 `SessionStore` 做内存会话存储。每个 session 保存：

- `session_id`
- `messages`
- `symptoms`
- `missing_fields`
- `next_question`
- `risk_level`
- `red_flags`
- `summary`
- `created_at`
- `updated_at`

为什么要做 session：

- LLM API 本身不会自动记住上一次 HTTP 请求。
- 多轮问诊必须保存历史输入和问诊状态。
- evaluate 阶段需要基于完整问诊上下文生成建议。
- 后续可以把内存存储替换成 Redis 或数据库。

当前内存版限制：

- 服务重启后会话丢失。
- 多进程、多实例部署时无法共享。
- 没有自动过期机制。

项目已经写了 `RedisSessionStore`，设计上使用：

```text
key: triage:session:{session_id}
value: JSON session
ttl: 24 小时
```

面试表达：

> MVP 阶段我用内存字典快速打通多轮问诊，后续用 Redis 保存短期 session，支持 TTL、多实例共享和服务重启后的状态恢复。长期问诊记录再考虑落数据库，避免把临时上下文和业务历史混在一起。

## 6. LLM 结构化输出设计

项目里 LLM 主要做四类任务：

- 首轮问诊信息抽取：症状、缺失字段、下一轮问题。
- 继续问诊状态更新：摘要、症状、缺失字段、是否继续追问。
- 红旗症状语义识别：识别“喘不上气”“胸口压着难受”等口语化表达。
- 最终分诊建议生成：结合 session、知识库和风险等级生成建议。

LLM 调用特点：

- 使用 `temperature=0`，提高结构化输出稳定性。
- system prompt 明确要求“只返回 JSON”。
- Prompt 中明确医疗边界：不诊断、不开药、信息不足继续追问。
- 用 `parse_json_object` 做 JSON 解析兜底。

JSON 解析兜底解决的问题：

模型有时不会严格返回纯 JSON，可能返回：

```text
```json
{...}
```
```

或者在 JSON 前后附带解释。`parse_json_object` 的处理策略是：

- 去掉 Markdown 代码块。
- 先尝试直接 `json.loads`。
- 失败后用正则从文本中提取 `{...}`。
- 仍失败则返回空字典，由业务层使用 fallback 默认值。

面试表达：

> 我没有假设模型永远遵守 Prompt，而是在服务层做了解析兜底和异常 fallback。这样即使 LLM 返回格式不规范或调用失败，接口仍能返回稳定结构，不会直接把异常暴露给调用方。

## 7. 红旗症状识别和风险合并

医疗分诊场景不能完全依赖 LLM，所以项目采用双层风险识别：

```text
规则关键词识别
  +
LLM / Tool Calling 语义识别
  ->
保守合并
```

规则层文件：

```text
app/core/risk_rules.py
app/service/risk_control_service.py
```

当前红旗关键词包括：

- 胸痛
- 胸闷
- 呼吸困难
- 喘不上气
- 意识模糊
- 抽搐
- 高热不退
- 昏迷
- 便血
- 呕血
- 咯血
- 严重头痛
- 剧烈腹痛
- 持续加重

LLM/Tool 层负责识别口语化表达，例如：

- “胸口像被压着”
- “喘不过来”
- “人有点迷糊”

合并策略在 `TriageService._merge_risk`：

- 规则判断为 `high`，最终就是 `high`。
- LLM 判断为 `high`，最终就是 `high`。
- LLM 失败返回 `unknown` 时，回退到规则结果。
- 红旗症状列表去重保留。

继续问诊时还有一个关键保护：

```text
如果 session 历史 risk_level 已经是 high，后续不能被降级。
```

面试表达：

> 医疗安全上我采用保守策略。规则层保证明确高危词不会漏掉，LLM 层补充口语化语义识别。最终合并时只要任一侧判断高风险，就按高风险处理，并且后续问诊不会把已经识别出的高风险降级。

## 8. ToolService 和工具能力

`ToolService` 是项目里的工具层统一入口，目前封装了：

- `extract_symptoms`：用 Function Call 方式抽取症状。
- `check_red_flags`：用 Function Call 方式判断红旗症状。
- `search_knowledge`：检索医学知识库。
- `book_appointment`：根据症状和风险推荐科室。

需要注意一个面试细节：


当前项目不是所有步骤都已经交给模型自主选择工具。更准确的说法是：

```text
当前阶段：后端编排工具调用 + 部分 OpenAI-compatible function calling。
后续阶段：用 LangGraph 或 Agent planner 让模型/工作流根据状态选择工具。
```

面试表达：

> 我先把问诊流程中的能力拆成工具，并用 `ToolService` 统一封装。当前主要是后端编排调用，部分工具已经使用 function calling schema。这样做的好处是接口稳定，后续接 LangGraph 时可以直接把这些工具变成节点或 tool calls。

## 9. RAG 和知识库设计

项目里有两套知识检索能力：

1. `app/tools/search_knowledge.py`：早期轻量关键词检索，适合解释 RAG MVP。
2. `app/service/vector_knowledge_service.py`：当前主链路使用的 ChromaDB 向量检索服务。

当前 `/triage/evaluate` 通过：

```python
self.tool_service.search_knowledge(query=query, top_k=3)
```

实际调用：

```text
ToolService.search_knowledge
  -> VectorKnowledgeService.search
  -> ChromaDB collection.query
```

知识库目录：

```text
data/knowledge/
  fever.md
  cough.md
  chest_pain.md
  W020190905516256771763.pdf
```

向量 RAG 构建流程：

```text
读取 data/knowledge/*.md
  -> 按 Markdown 段落切 chunk
  -> 提取 title、source、chunk_index metadata
  -> 使用 text2vec-base-chinese 生成 embedding
  -> 写入 ChromaDB medical_knowledge collection
  -> evaluate 阶段按 query 检索 top_k
  -> 返回 results 和 references
  -> 注入最终分诊 Prompt
```

`VectorKnowledgeService.search` 返回结构：

```json
{
  "query": "胸痛、呼吸困难",
  "results": [
    {
      "keyword": "胸痛",
      "content": "知识片段",
      "source": "chest_pain.md",
      "score": 0.31
    }
  ],
  "references": ["chest_pain.md"]
}
```

RAG 的项目价值：

- 不让模型完全依赖内部知识。
- 可以把本地医学知识作为依据注入 Prompt。
- `references` 方便解释和追踪。
- 后续可以替换为 pgvector、FAISS 或更完整的检索链路。

面试表达：

> 我先用 Markdown 建了一个小型医疗知识库，然后升级到 ChromaDB 向量检索。evaluate 阶段会根据症状构造 query，检索相关 chunk，把结果注入 Prompt，并把来源文件作为 references 返回。这样最终建议不是完全凭模型生成，而是结合了问诊上下文和外部知识。

## 10. 科室推荐和模拟挂号

科室推荐工具在：

```text
app/tools/book_appointment.py
```

规则示例：

- 胸痛、胸闷 -> 心内科
- 呼吸困难、咳嗽、气促 -> 呼吸内科
- 发热、高热 -> 发热门诊
- 腹痛、腹泻、呕吐 -> 消化内科
- 头痛、头晕 -> 神经内科
- 意识模糊、抽搐、昏迷 -> 急诊科

关键逻辑：

```text
如果 risk_level == high:
  直接推荐急诊科
否则:
  根据症状关键词匹配普通门诊科室
```

面试表达：

> 科室推荐我没有完全交给 LLM，而是先用规则做一个稳定的模拟挂号工具。尤其是高风险场景会直接推荐急诊科，避免模型在高风险情况下给出过于温和的建议。

## 11. 测试覆盖

当前测试集中包含：

```text
tests/test_triage_api.py
tests/test_triage_service.py
tests/test_tools.py
tests/test_rag.py
```

已覆盖的关键场景：

- `/triage/start -> /triage/continue -> /triage/evaluate` 完整接口流程。
- 普通低风险发热咳嗽输入。
- 胸痛、喘不上气等高风险输入。
- 无效 `session_id` 返回 404。
- LLM 红旗判断失败时规则仍然生效。
- 高风险 session 后续不会被降级。
- 挂号工具高风险推荐急诊。
- 知识检索能返回 `results` 和 `references`。
- ChromaDB 索引能从 Markdown 构建 chunk。

面试表达：

> 我用 pytest 覆盖了主流程和关键安全场景。特别是医疗分诊里最重要的是假阴性风险，所以我专门测了 LLM 失败时规则仍能识别胸痛，以及高风险会话不能在后续被降级。

## 12. 项目亮点

### 12.1 工程分层清晰

接口、Schema、业务编排、LLM 调用、工具、知识库、规则分别拆开。这样项目虽然是 Demo，但结构接近真实后端服务。

### 12.2 医疗安全边界明确

Prompt 和业务逻辑都强调：

- 不做明确诊断。
- 不开药。
- 信息不足时继续追问。
- 高风险优先建议线下或急诊。

### 12.3 风险识别不完全依赖 LLM

规则和 LLM 双层识别，降低模型漏判红旗症状的风险。

### 12.4 支持多轮问诊状态

通过 session 管理历史消息、症状、摘要、风险和红旗症状，完成从单轮问答到多轮 Agent 的过渡。

### 12.5 已接入知识增强

从本地 Markdown 知识库到 ChromaDB 向量检索，evaluate 阶段能返回 references。

### 12.6 为 LangGraph 和生产化预留扩展点

已有 `agent/triage_graph.py`、`RedisSessionStore`、`ToolService`、`model/` 等扩展位置，后续可以继续升级。

## 13. 面试时怎么讲项目架构

推荐讲法：

> 这个项目我按后端服务来拆。最外层是 FastAPI 路由，定义了 start、continue、evaluate 三个接口。请求体和响应体都用 Pydantic 管理。核心业务放在 `TriageService`，它负责编排 LLM 抽取、风险控制、session 读写、知识库检索和科室推荐。LLM 调用统一封装在 `LLMService`，Prompt 放在 `prompt_manager`，风险规则放在 `risk_rules`。工具能力由 `ToolService` 管理，包括红旗识别、知识检索和模拟挂号。这样后续接 Redis、LangGraph 或替换向量库时，只需要换局部模块。

## 14. 面试时怎么讲主流程

推荐讲法：

> 用户第一次输入症状时，`/triage/start` 会调用 LLM 抽取症状、缺失信息和下一轮追问，同时用规则和 LLM 检查红旗症状。如果发现胸痛、呼吸困难这类高风险信号，就把风险等级设为 high。然后创建 session 保存上下文。用户继续回答时，`/triage/continue` 会根据 session 更新摘要和风险状态。最后 `/triage/evaluate` 读取完整 session，检索知识库，推荐科室，再让 LLM 基于问诊记录和知识库生成保守的分诊建议。

## 15. 高频面试追问和项目回答

### Q1：为什么不用单纯 LLM 判断风险？

回答：

> 医疗场景里高风险漏判成本很高，不能完全依赖 LLM。LLM 可能受 Prompt、表达方式和调用异常影响，所以我加了确定性规则兜底。规则负责识别明确红旗词，LLM 负责识别口语化表达，最后用保守策略合并，只要任一侧判断高风险，就按高风险处理。

### Q2：为什么要用 RAG？

回答：

> 如果完全依赖模型内部知识，建议不可追踪，也不方便更新。RAG 可以把项目自己的医学知识库检索出来注入 Prompt，并返回 references。知识更新时只需要更新文档和索引，不需要微调模型。

### Q3：RAG 的完整链路是什么？

回答：

> 我这里的链路是读取 `data/knowledge/*.md`，按 Markdown 段落切 chunk，写入 ChromaDB collection。evaluate 阶段根据症状构造 query，检索 top_k 相关 chunk，把检索结果放进 Prompt，同时把 source 文件名作为 references 返回。

### Q4：chunk 为什么按段落切？

回答：

> 当前知识库规模小，Markdown 文档结构清晰，所以先按段落切，能保持一个知识点的完整性。如果文档变长，后续会按标题层级、段落长度和 overlap 做更精细切分，并用 recall@k 和最终建议质量评估 chunk 策略。

### Q5：top_k 怎么选？

回答：

> 当前项目知识库小，先用 `top_k=3`，既能提供足够上下文，又不会让 Prompt 太长。后续会根据检索命中率、噪声比例、最终建议质量和 token 成本调参。

### Q6：Tool Calling 和普通函数调用有什么区别？

回答：

> 普通函数调用是后端自己决定调用哪个函数。Tool Calling 是模型根据上下文输出结构化 tool call，后端解析参数并执行工具，再把结果交给模型或业务流程。当前项目已经把能力封装成工具，并在部分红旗检查里使用 function calling schema；下一步会通过 LangGraph 或更完整的 Agent 流程让工具选择更自动化。

### Q7：为什么要有 JSON 解析兜底？

回答：

> 即使 Prompt 要求只返回 JSON，模型也可能返回代码块或解释文本。如果服务层直接 `json.loads`，接口会不稳定。所以我做了 `parse_json_object`，先清理代码块，再直接解析，失败后从文本中提取 JSON 对象，最后还有业务 fallback。

### Q8：项目目前有什么不足？

回答：

> 第一，内存 session 不适合生产，后续要切 Redis。第二，知识库规模还小，需要扩充医学文档和评估集。第三，LangGraph 目前还是预留示例，主链路还没有完全图编排。第四，配置里需要清理默认 API key，改成完全从环境变量读取。第五，RAG 还需要做系统评估，比如 recall@k、references 正确率和最终建议是否 grounded。

## 16. 可以主动展示的 Demo

### 16.1 低风险流程

输入：

```text
我发热两天了，还一直咳嗽
```

展示点：

- 抽取 `发热`、`咳嗽`。
- 追问体温、持续时间、是否呼吸困难。
- 风险等级低。
- evaluate 检索 fever/cough 相关知识。
- 推荐呼吸内科或发热门诊。

### 16.2 高风险流程

输入：

```text
我胸口像被压着，而且有点喘不上气
```

展示点：

- 识别胸痛/胸闷和呼吸困难语义。
- 风险等级为 high。
- 红旗症状不被后续回答降级。
- 推荐急诊科。
- 建议尽快线下或急诊评估。

### 16.3 信息不足流程

输入：

```text
我不舒服
```

展示点：

- 系统不会直接诊断。
- 会追问持续时间、具体部位、严重程度、伴随症状等。
- 体现医疗问诊的安全边界。

## 17. 简历描述候选版本

简洁版：

> 基于 FastAPI 和 OpenAI-compatible LLM 实现医疗问诊分诊 Agent，支持多轮问诊、红旗症状识别、ChromaDB 向量 RAG、科室推荐和结构化分诊建议输出。

详细版：

> 设计并实现医疗问诊分诊 Agent Demo，基于 FastAPI 提供 `/triage/start`、`/triage/continue`、`/triage/evaluate` 三条接口；使用 Pydantic 定义请求与响应模型，通过 session 维护多轮问诊上下文；封装 LLMService 完成症状抽取、问诊摘要更新和最终建议生成，并通过 JSON 解析兜底提高结构化输出稳定性；结合规则引擎与 LLM Tool Calling 识别红旗症状，采用保守合并策略避免高风险降级；构建 Markdown 医疗知识库并接入 ChromaDB 向量检索，在最终评估阶段注入知识片段并返回 references；封装 ToolService 支持知识检索和模拟挂号科室推荐，并使用 pytest 覆盖接口主流程、高风险识别、未知 session 和 RAG 检索等关键场景。

## 18. 下一步最值得做什么

### 18.1 清理配置安全

当前 `app/core/config.py` 里还有默认 API key。面试或提交代码前应改成完全从环境变量读取，并提供 `.env.example`。

### 18.2 把 SessionStore 切成工厂模式

增加配置：

```text
SESSION_STORE_TYPE=memory|redis
```

让 `TriageService` 不关心底层是内存还是 Redis。

### 18.3 接入 LangGraph 主链路

把 evaluate 或完整问诊流程拆成节点：

```text
load_session
  -> check_risk
  -> retrieve_knowledge
  -> recommend_department
  -> generate_advice
  -> format_response
```

高风险分支：

```text
check_risk
  -> if high
  -> emergency_advice
```

### 18.4 做 RAG 评估集

构建小型测试集：

```json
{
  "query": "胸口压着难受，喘不上气",
  "expected_reference": "chest_pain.md",
  "expected_risk_level": "high",
  "expected_department": "急诊科"
}
```

评估指标：

- recall@k
- references 命中率
- red_flags recall
- department accuracy
- JSON valid rate

### 18.5 扩充知识库

建议新增：

- 腹痛
- 头痛
- 儿童发热
- 老年胸痛
- 孕妇腹痛
- 呼吸困难
- 过敏反应

统一 Markdown 格式：

```markdown
# 症状名称

## 常见原因

## 需要追问

## 红旗症状

## 建议科室

## 安全建议
```

## 19. 当前阶段总结

doctorTing 当前已经具备一个 Agent 项目的核心雏形：

```text
多轮问诊
  + 结构化输出
  + 风险识别
  + 工具封装
  + 本地知识库
  + 向量 RAG
  + 科室推荐
  + 自动化测试
```

它最适合在面试中展示三类能力：

- 后端工程能力：FastAPI、Pydantic、分层设计、session、测试。
- LLM 应用能力：Prompt、结构化输出、Tool Calling、JSON 兜底、RAG。
- Agent 安全意识：医疗边界、红旗症状、规则兜底、高风险不降级。

最终推荐的面试收束话术：

> 这个项目的重点不是做一个医疗诊断系统，而是展示我如何把 LLM 能力工程化：把自然语言输入转成结构化状态，用 session 支持多轮流程，用规则和 LLM 共同处理高风险，用 RAG 引入外部知识，用工具层封装可扩展能力，并用测试保证关键安全场景可回归。
